var aventuras = false;
var quemada = false;
var superpoder = false;
var friendsoluciona = false;

	
var b1;

function setup() {

  frameRate(10);
  b1 = createButton('Amiga idea');
  b1.mousePressed(function(){aventuras=true;});
}

function draw() {

    //aventuras a tope ¿?
    if ( aventuras == true ){

    	  ///superpoder.leer tu mente ¿?
    	  if ( superpoder == true ){
	  	
		////friend para que lo solucione
          	if ( friendsoluciona == true ){
	
				/*falten 3 pasos
				createP("dragon");
   			 	  }
      	else {
        createP("wallapopme!");
      }
    }
    else{
      createP("enchufame nen!");


    }

}
